@model List<Student>
<table class="table table-bordered table-hover">
	@foreach(var i in Model){
		<tr>
			<td>@i.Id</td>
			<td>@i.Name</td>
			<td>@i.Title</td>
			<td>
				<img src="~/image/@i.ImageURL" style="width:100px" />
			</td>
		</tr>
	}
</table>
